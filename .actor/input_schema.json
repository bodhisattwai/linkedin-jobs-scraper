{
  "title": "LinkedIn Jobs Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQueries": {
      "title": "Search Queries",
      "type": "array",
      "description": "List of job titles or keywords to search for",
      "editor": "stringList",
      "prefill": ["Software Engineer", "Data Scientist"],
      "default": ["Software Engineer"]
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location to search jobs in (country, city, or region)",
      "editor": "textfield",
      "prefill": "United States",
      "default": "United States"
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of jobs to scrape",
      "editor": "number",
      "default": 100,
      "minimum": 1,
      "maximum": 1000
    },
    "maxConcurrency": {
      "title": "Maximum Concurrency",
      "type": "integer",
      "description": "Number of concurrent requests",
      "editor": "number",
      "default": 2,
      "minimum": 1,
      "maximum": 5
    },
    "minDelayBetweenRequests": {
      "title": "Min Delay Between Requests (ms)",
      "type": "integer",
      "description": "Minimum delay in milliseconds between requests to avoid detection",
      "editor": "number",
      "default": 2000,
      "minimum": 1000
    },
    "maxDelayBetweenRequests": {
      "title": "Max Delay Between Requests (ms)",
      "type": "integer",
      "description": "Maximum delay in milliseconds between requests",
      "editor": "number",
      "default": 5000,
      "minimum": 1000
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for the scraper",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    },
    "linkedinCookies": {
      "title": "LinkedIn Session Cookies (Optional)",
      "type": "string",
      "description": "JSON-formatted LinkedIn session cookies for authenticated scraping",
      "editor": "textarea"
    },
    "extendOutputFunction": {
      "title": "Custom Output Function",
      "type": "string",
      "description": "Custom JavaScript function to extend output",
      "editor": "javascript",
      "default": ""
    },
    "customData": {
      "title": "Custom Data",
      "type": "object",
      "description": "Custom data to include with each job record",
      "editor": "json",
      "default": {}
    },
    "debugMode": {
      "title": "Debug Mode",
      "type": "boolean",
      "description": "Enable detailed logging for debugging",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": ["searchQueries", "location"]
}